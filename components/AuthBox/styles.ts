import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  bottomBlock: {
    flexDirection: "row",
    alignItems: "center",
  },
  defaultText: {
    fontSize: 20,
    color: "#fff",
  },
  input: {
    width: "80%",
    fontSize: 20,
    padding: 10,
    paddingBottom: 5,
    marginVertical: 5,
    backgroundColor: "#99c",
  },
});
