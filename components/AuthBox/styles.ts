import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bottomBlock: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  defaultText: {
    fontSize: 20,
    color: '#fff',
  },
  input: {
    width: '80%',
    fontSize: 20,
    marginVertical: 5,
  },
});
