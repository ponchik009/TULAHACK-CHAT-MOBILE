import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  logo: {
    flexDirection: "row",
    alignItems: "center",
    flex: 1,
  },
  main: {
    flex: 2,
    alignItems: "center",
    justifyContent: "flex-start",
    width: "100%",
  },
  logoText: {
    fontSize: 30,
    color: "#fff",
  },
});
